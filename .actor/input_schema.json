{
  "title": "LinkedIn Batch Wrapper Input",
  "description": "Input for self-contained LinkedIn scraping actor",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "profileUrls": {
      "title": "LinkedIn Profile URLs",
      "description": "List of LinkedIn profiles to scrape",
      "type": "array",
      "items": {
        "type": "string"
      },
      "editor": "stringList"
    },
    "maxPostsPerProfile": {
      "title": "Max Posts Per Profile",
      "description": "Maximum number of posts to collect per profile",
      "type": "integer",
      "default": 10
    },
    "maxScrolls": {
      "title": "Max Scrolls",
      "description": "Maximum number of scroll actions per profile page",
      "type": "integer",
      "default": 8
    },
    "includeReposts": {
      "title": "Include Reposts",
      "description": "Whether to include reposted/shared content",
      "type": "boolean",
      "default": false
    },
    "debug": {
      "title": "Debug Mode",
      "description": "Enable verbose logging",
      "type": "boolean",
      "default": false
    },
    "loginCookiesJson": {
      "title": "LinkedIn Cookies JSON",
      "description": "Optional exported LinkedIn cookies array as JSON string, used if the recent activity page requires authentication",
      "type": "string",
      "editor": "textarea"
    }
  },
  "required": ["profileUrls"]
}
``
