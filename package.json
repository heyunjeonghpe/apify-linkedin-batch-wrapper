{
  "name": "apify-linkedin-batch-wrapper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Batch wrapper actor that calls an existing Apify LinkedIn scraping task across many LinkedIn profiles and returns normalized dataset items.",
  "scripts": {
    "start": "node src/main.js"
  },
  "dependencies": {
    "apify": "^3.4.0"
  }
}
